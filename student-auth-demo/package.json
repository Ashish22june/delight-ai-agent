{
  "name": "delight-student-auth-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Demo: create a Delight AI Agent student account (if missing) then load the Messenger with an authenticated session.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
